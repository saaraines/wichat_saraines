sonar.projectKey=saaraines_wichat_saraines
sonar.organization=saaraines

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=wichat_saraines
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=.

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Coverage reports
sonar.javascript.lcov.reportPaths=gatewayservice/coverage/lcov.info,llmservice/coverage/lcov.info,questionservice/coverage/lcov.info,users/authservice/coverage/lcov.info,users/userservice/coverage/lcov.info,webapp/coverage/lcov.info

# Exclude test files and node_modules
sonar.exclusions=**/node_modules/**,**/*.test.js,**/*.test.jsx,**/coverage/**,**/e2e/**